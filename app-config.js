(()=>{const settings=JSON.parse(localStorage.getItem('vm_admin_settings')||'{}');const defaultLogo='https://raw.githubusercontent.com/elielscarabelliserafim507-lab/Viacao-Motta/main/assets/logo-vm-approved.png';if(settings.primary)document.documentElement.style.setProperty('--green',settings.primary);if(settings.accent){document.documentElement.style.setProperty('--yellow',settings.accent);document.documentElement.style.setProperty('--yellow2',settings.accent)}if(settings.background)document.documentElement.style.setProperty('--bg',settings.background);const logo=settings.logoUrl||defaultLogo;document.querySelectorAll('img[alt*="Viação Motta"],img[alt*="Logo Viação Motta"]').forEach(img=>img.src=logo);const form=document.querySelector('#loginForm');if(form){form.addEventListener('submit',e=>{const raw=(document.querySelector('#loginIdentity')?.value||'').trim();if(!raw.includes('@'))return;const email=raw.toLowerCase();const drivers=JSON.parse(localStorage.getItem('vm_drivers')||'[]');const driver=drivers.find(d=>String(d.email||'').toLowerCase()===email);if(driver&&driver.authorized===false){e.preventDefault();e.stopImmediatePropagation();const s=document.querySelector('#loginStatus');if(s)s.textContent='Este e-mail está aguardando autorização do administrador.'}},true)}})();